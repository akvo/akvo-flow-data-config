# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users
- import: csv_fix

transformers:

- kind: AccessPointScoreComputationItem
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('AccessPointScoreComputationItem', key_is_id=True)

    - property: scoreComputationItems_INTEGER_IDX
      external_name: scoreComputationItems_INTEGER_IDX
      # Type: Integer Stats: 1927 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: scoreDetailMessage
      external_name: scoreDetailMessage
      # Type: String Stats: 1927 properties of this type in this kind.

    - property: scoreItem
      external_name: scoreItem
      # Type: Integer Stats: 1927 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: AccessPointScoreDetail
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header  
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('AccessPointScoreDetail', key_is_id=True)

    - property: accessPointId
      external_name: accessPointId
      # Type: Integer Stats: 1927 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: computationDate
      external_name: computationDate
      # Type: Date/Time Stats: 1927 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 1927 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 1927 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: score
      external_name: score
      # Type: Integer Stats: 1927 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: status
      external_name: status
      # Type: String Stats: 1927 properties of this type in this kind.


- kind: AccessPointStatusSummary
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('AccessPointStatusSummary', key_is_id=True)

    - property: community
      external_name: community
      # Type: String Stats: 101 properties of this type in this kind.

    - property: count
      external_name: count
      # Type: Integer Stats: 101 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: country
      external_name: country
      # Type: String Stats: 101 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 101 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 101 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: status
      external_name: status
      # Type: String Stats: 100 properties of this type in this kind.

    - property: type
      external_name: type
      # Type: String Stats: 101 properties of this type in this kind.

    - property: year
      external_name: year
      # Type: String Stats: 101 properties of this type in this kind.


- kind: AccessPoint
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('AccessPoint', key_is_id=True)

    - property: accessPointCode
      external_name: accessPointCode
      # Type: String Stats: 1364 properties of this type in this kind.

    - property: altitude
      external_name: altitude
      # Type: Float Stats: 1363 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: collectionDate
      external_name: collectionDate
      # Type: Date/Time Stats: 1364 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: communityCode
      external_name: communityCode
      # Type: String Stats: 1364 properties of this type in this kind.

    - property: communityName
      external_name: communityName
      # Type: String Stats: 2 properties of this type in this kind.

    - property: constructionDateYear
      external_name: constructionDateYear
      # Type: String Stats: 1 properties of this type in this kind.

    - property: costPer
      external_name: costPer
      # Type: Float Stats: 271 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: countryCode
      external_name: countryCode
      # Type: String Stats: 21 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 1364 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: currentManagementStructurePoint
      external_name: currentManagementStructurePoint
      # Type: String Stats: 1 properties of this type in this kind.

    - property: district
      external_name: district
      # Type: String Stats: 3 properties of this type in this kind.

    - property: extimatedPopulation
      external_name: extimatedPopulation
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: geocells
      external_name: geocells
      # Type: String Stats: 16861 properties of this type in this kind.
      import_transform: transform.regexp_to_list('(\w+)')

    - property: hasSystemBeenDown1DayFlag
      external_name: hasSystemBeenDown1DayFlag
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 1364 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: latitude
      external_name: latitude
      # Type: Float Stats: 1364 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: longitude
      external_name: longitude
      # Type: Float Stats: 1364 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: numberOutsideAcceptableDistance
      external_name: numberOutsideAcceptableDistance
      # Type: Integer Stats: 1362 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: numberWithinAcceptableDistance
      external_name: numberWithinAcceptableDistance
      # Type: Integer Stats: 1362 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: photoURL
      external_name: photoURL
      # Type: String Stats: 5 properties of this type in this kind.

    - property: pointPhotoCaption
      external_name: pointPhotoCaption
      # Type: String Stats: 2 properties of this type in this kind.

    - property: pointStatus
      external_name: pointStatus
      # Type: String Stats: 608 properties of this type in this kind.

    - property: pointType
      external_name: pointType
      # Type: String Stats: 1364 properties of this type in this kind.

    - property: scoreComputationDate
      external_name: scoreComputationDate
      # Type: Date/Time Stats: 606 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: score
      external_name: score
      # Type: Integer Stats: 606 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: sub1
      external_name: sub1
      # Type: String Stats: 2 properties of this type in this kind.

    - property: sub2
      external_name: sub2
      # Type: String Stats: 2 properties of this type in this kind.

    - property: surveyId
      external_name: surveyId
      # Type: Integer Stats: 1362 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyInstanceId
      external_name: surveyInstanceId
      # Type: Integer Stats: 1362 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: typeTechnologyString
      external_name: typeTechnologyString
      # Type: String Stats: 4 properties of this type in this kind.

    - property: waterAvailableDayVisitFlag
      external_name: waterAvailableDayVisitFlag
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: waterForPeopleProjectFlag
      external_name: waterForPeopleProjectFlag
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: whoRepairsPoint
      external_name: whoRepairsPoint
      # Type: String Stats: 1 properties of this type in this kind.


- kind: Community
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Community', key_is_id=True)

    - property: communityCode
      external_name: communityCode
      # Type: String Stats: 274 properties of this type in this kind.

    - property: countryCode
      external_name: countryCode
      # Type: String Stats: 275 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 275 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 275 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lat
      external_name: lat
      # Type: Float Stats: 207 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: lon
      external_name: lon
      # Type: Float Stats: 207 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: name
      external_name: name
      # Type: String Stats: 207 properties of this type in this kind.


- kind: Coordinate
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Coordinate', key_is_id=True)

    - property: coordinates_INTEGER_IDX
      external_name: coordinates_INTEGER_IDX
      # Type: Integer Stats: 29469 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: x
      external_name: x
      # Type: Float Stats: 29469 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: y
      external_name: y
      # Type: Float Stats: 29469 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)


- kind: Country
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Country', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 12 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: displayName
      external_name: displayName
      # Type: String Stats: 12 properties of this type in this kind.

    - property: isoAlpha2Code
      external_name: isoAlpha2Code
      # Type: String Stats: 12 properties of this type in this kind.

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 12 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: String Stats: 12 properties of this type in this kind.


- kind: DeviceFiles
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('DeviceFiles', key_is_id=True)

    - property: URI
      external_name: URI
      # Type: String Stats: 1290 properties of this type in this kind.

    - property: checksum
      external_name: checksum
      # Type: String Stats: 961 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 1290 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 1290 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: phoneNumber
      external_name: phoneNumber
      # Type: String Stats: 960 properties of this type in this kind.

    - property: processDate
      external_name: processDate
      # Type: String Stats: 1290 properties of this type in this kind.

    - property: processedStatus
      external_name: processedStatus
      # Type: String Stats: 1290 properties of this type in this kind.

    - property: processingMessageText
      external_name: processingMessageText
      # Type: Text Stats: 73 properties of this type in this kind.
      import_transform: db.Text

    - property: surveyInstanceId
      external_name: surveyInstanceId
      # Type: Integer Stats: 1124 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: uploadDateTime
      external_name: uploadDateTime
      # Type: Date/Time Stats: 1290 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: DeviceSurveyJobQueue
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('DeviceSurveyJobQueue', key_is_id=True)

    - property: assignmentId
      external_name: assignmentId
      # Type: Integer Stats: 30 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: devicePhoneNumber
      external_name: devicePhoneNumber
      # Type: String Stats: 30 properties of this type in this kind.

    - property: effectiveEndDate
      external_name: effectiveEndDate
      # Type: Date/Time Stats: 30 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: effectiveStartDate
      external_name: effectiveStartDate
      # Type: Date/Time Stats: 30 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: language
      external_name: language
      # Type: String Stats: 6 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 30 properties of this type in this kind.

    - property: surveyDistributionStatus
      external_name: surveyDistributionStatus
      # Type: String Stats: 30 properties of this type in this kind.

    - property: surveyID
      external_name: surveyID
      # Type: Integer Stats: 30 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Device
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Device', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 54 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: deviceIdentifier
      external_name: deviceIdentifier
      # Type: String Stats: 16 properties of this type in this kind.

    - property: deviceType
      external_name: deviceType
      # Type: String Stats: 54 properties of this type in this kind.

    - property: gallatinSoftwareManifest
      external_name: gallatinSoftwareManifest
      # Type: String Stats: 50 properties of this type in this kind.

    - property: lastKnownAccuracy
      external_name: lastKnownAccuracy
      # Type: Float Stats: 45 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: lastKnownLat
      external_name: lastKnownLat
      # Type: Float Stats: 45 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: lastKnownLon
      external_name: lastKnownLon
      # Type: Float Stats: 45 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: lastLocationBeaconTime
      external_name: lastLocationBeaconTime
      # Type: Date/Time Stats: 50 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 54 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: phoneNumber
      external_name: phoneNumber
      # Type: String Stats: 54 properties of this type in this kind.


- kind: EditorialPage
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('EditorialPage', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: targetFileName
      external_name: targetFileName
      # Type: String Stats: 1 properties of this type in this kind.

    - property: template
      external_name: template
      # Type: Text Stats: 1 properties of this type in this kind.
      import_transform: db.Text


- kind: GeoRegion
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('GeoRegion', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 26 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 26 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: latitiude
      external_name: latitiude
      # Type: Float Stats: 26 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: longitude
      external_name: longitude
      # Type: Float Stats: 26 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: name
      external_name: name
      # Type: String Stats: 26 properties of this type in this kind.

    - property: order
      external_name: order
      # Type: Integer Stats: 26 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: uuid
      external_name: uuid
      # Type: String Stats: 26 properties of this type in this kind.


- kind: Geometry
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Geometry', key_is_id=True)

    - property: centroidLat
      external_name: centroidLat
      # Type: Float Stats: 6477 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: centroidLon
      external_name: centroidLon
      # Type: Float Stats: 6477 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: type
      external_name: type
      # Type: String Stats: 8146 properties of this type in this kind.

    - property: wktText
      external_name: wktText
      # Type: Text Stats: 8146 properties of this type in this kind.
      import_transform: db.Text


- kind: MappingSpreadsheetDefinition
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('MappingSpreadsheetDefinition', key_is_id=True)

    - property: columnMap
      external_name: columnMap
      # Type: Blob Stats: 3 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 3 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 3 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: spreadsheetURL
      external_name: spreadsheetURL
      # Type: String Stats: 3 properties of this type in this kind.


- kind: Message
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Message', key_is_id=True)

    - property: actionAbout
      external_name: actionAbout
      # Type: String Stats: 818 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 818 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 818 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: message
      external_name: message
      # Type: Text Stats: 2 properties of this type in this kind.
      import_transform: db.Text

    - property: objectId
      external_name: objectId
      # Type: Integer Stats: 818 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: objectTitle
      external_name: objectTitle
      # Type: String Stats: 815 properties of this type in this kind.

    - property: shortMessage
      external_name: shortMessage
      # Type: String Stats: 816 properties of this type in this kind.

    - property: transactionUUID
      external_name: transactionUUID
      # Type: String Stats: 764 properties of this type in this kind.

    - property: userName
      external_name: userName
      # Type: String Stats: 53 properties of this type in this kind.


- kind: Metric
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Metric', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: group
      external_name: group
      # Type: String Stats: 2 properties of this type in this kind.

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: String Stats: 2 properties of this type in this kind.

    - property: organization
      external_name: organization
      # Type: String Stats: 2 properties of this type in this kind.

    - property: valueType
      external_name: valueType
      # Type: String Stats: 2 properties of this type in this kind.


- kind: NotificationHistory
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('NotificationHistory', key_is_id=True)

    - property: checksum
      external_name: checksum
      # Type: String Stats: 2 properties of this type in this kind.

    - property: count
      external_name: count
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: entityId
      external_name: entityId
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: lastNotification
      external_name: lastNotification
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: type
      external_name: type
      # Type: String Stats: 2 properties of this type in this kind.


- kind: NotificationSubscription
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('NotificationSubscription', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 5 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: entityId
      external_name: entityId
      # Type: Integer Stats: 5 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: expiryDate
      external_name: expiryDate
      # Type: Date/Time Stats: 5 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 5 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: notificationDestination
      external_name: notificationDestination
      # Type: String Stats: 5 properties of this type in this kind.

    - property: notificationMethod
      external_name: notificationMethod
      # Type: String Stats: 5 properties of this type in this kind.

    - property: notificationOption
      external_name: notificationOption
      # Type: String Stats: 5 properties of this type in this kind.

    - property: notificationType
      external_name: notificationType
      # Type: String Stats: 5 properties of this type in this kind.


- kind: OGRFeature
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('OGRFeature', key_is_id=True)

    - property: centroidLat
      external_name: centroidLat
      # Type: Float Stats: 4649 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: centroidLon
      external_name: centroidLon
      # Type: Float Stats: 4649 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: countryCode
      external_name: countryCode
      # Type: String Stats: 4649 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 4649 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: datumIdentifier
      external_name: datumIdentifier
      # Type: String Stats: 4649 properties of this type in this kind.

    - property: featureType
      external_name: featureType
      # Type: String Stats: 4649 properties of this type in this kind.

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 4649 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: String Stats: 4649 properties of this type in this kind.

    - property: pop2005
      external_name: pop2005
      # Type: Integer Stats: 246 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: projectCoordinateSystemIdentifier
      external_name: projectCoordinateSystemIdentifier
      # Type: String Stats: 4649 properties of this type in this kind.

    - property: spheroid
      external_name: spheroid
      # Type: Float Stats: 4649 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: sub1
      external_name: sub1
      # Type: String Stats: 4403 properties of this type in this kind.

    - property: sub2
      external_name: sub2
      # Type: String Stats: 4403 properties of this type in this kind.

    - property: x1
      external_name: x1
      # Type: Float Stats: 4649 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: x2
      external_name: x2
      # Type: Float Stats: 4649 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: y1
      external_name: y1
      # Type: Float Stats: 4649 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: y2
      external_name: y2
      # Type: Float Stats: 4649 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)


- kind: Permission
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Permission', key_is_id=True)

    - property: code
      external_name: code
      # Type: String Stats: 9 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: String Stats: 9 properties of this type in this kind.


- kind: QuestionAnswerStore
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('QuestionAnswerStore', key_is_id=True)

    - property: arbitratyNumber
      external_name: arbitratyNumber
      # Type: Integer Stats: 64138 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: collectionDate
      external_name: collectionDate
      # Type: Date/Time Stats: 64124 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 64138 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 64138 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: questionID
      external_name: questionID
      # Type: String Stats: 64138 properties of this type in this kind.

    - property: scoredValue
      external_name: scoredValue
      # Type: String Stats: 63839 properties of this type in this kind.

    - property: strength
      external_name: strength
      # Type: String Stats: 63839 properties of this type in this kind.

    - property: surveyId
      external_name: surveyId
      # Type: Integer Stats: 64138 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyInstanceId
      external_name: surveyInstanceId
      # Type: Integer Stats: 64138 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: type
      external_name: type
      # Type: String Stats: 64138 properties of this type in this kind.

    - property: value
      external_name: value
      # Type: String Stats: 64137 properties of this type in this kind.


- kind: QuestionGroup
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('QuestionGroup', key_is_id=True)

    - property: code
      external_name: code
      # Type: String Stats: 69 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 69 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 69 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: String Stats: 48 properties of this type in this kind.

    - property: order
      external_name: order
      # Type: Integer Stats: 69 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: path
      external_name: path
      # Type: String Stats: 69 properties of this type in this kind.

    - property: surveyId
      external_name: surveyId
      # Type: Integer Stats: 69 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: QuestionOption
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('QuestionOption', key_is_id=True)

    - property: code
      external_name: code
      # Type: String Stats: 1195 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 1769 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 1769 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: order
      external_name: order
      # Type: Integer Stats: 1769 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: questionId
      external_name: questionId
      # Type: Integer Stats: 1769 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: text
      external_name: text
      # Type: String Stats: 1769 properties of this type in this kind.


- kind: Question
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Question', key_is_id=True)

    - property: allowDecimal
      external_name: allowDecimal
      # Type: Boolean Stats: 445 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: allowMultipleFlag
      external_name: allowMultipleFlag
      # Type: Boolean Stats: 193 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: allowOtherFlag
      external_name: allowOtherFlag
      # Type: Boolean Stats: 207 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: allowSign
      external_name: allowSign
      # Type: Boolean Stats: 445 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: collapseable
      external_name: collapseable
      # Type: Boolean Stats: 506 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 318 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: dependentFlag
      external_name: dependentFlag
      # Type: Boolean Stats: 362 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dependentQuestionAnswer
      external_name: dependentQuestionAnswer
      # Type: String Stats: 315 properties of this type in this kind.

    - property: dependentQuestionId
      external_name: dependentQuestionId
      # Type: Integer Stats: 315 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: immutable
      external_name: immutable
      # Type: Boolean Stats: 506 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: isName
      external_name: isName
      # Type: Boolean Stats: 445 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 318 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: mandatoryFlag
      external_name: mandatoryFlag
      # Type: Boolean Stats: 673 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: maxVal
      external_name: maxVal
      # Type: Float Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: minVal
      external_name: minVal
      # Type: Float Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: order
      external_name: order
      # Type: Integer Stats: 734 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: path
      external_name: path
      # Type: String Stats: 734 properties of this type in this kind.

    - property: questionGroupId
      external_name: questionGroupId
      # Type: Integer Stats: 734 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: referenceId
      external_name: referenceId
      # Type: String Stats: 203 properties of this type in this kind.

    - property: surveyId
      external_name: surveyId
      # Type: Integer Stats: 734 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: text
      external_name: text
      # Type: String Stats: 734 properties of this type in this kind.

    - property: tip
      external_name: tip
      # Type: String Stats: 476 properties of this type in this kind.

    - property: type
      external_name: type
      # Type: String Stats: 734 properties of this type in this kind.

    - property: validationRule
      external_name: validationRule
      # Type: String Stats: 11 properties of this type in this kind.


- kind: RemoteStacktrace
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('RemoteStacktrace', key_is_id=True)

    - property: acknowleged
      external_name: acknowleged
      # Type: Boolean Stats: 2204 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 2204 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: deviceIdentifier
      external_name: deviceIdentifier
      # Type: String Stats: 2204 properties of this type in this kind.

    - property: errorDate
      external_name: errorDate
      # Type: Date/Time Stats: 2204 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 2204 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: phoneNumber
      external_name: phoneNumber
      # Type: String Stats: 2204 properties of this type in this kind.

    - property: softwareVersion
      external_name: softwareVersion
      # Type: String Stats: 2204 properties of this type in this kind.

    - property: stackTrace
      external_name: stackTrace
      # Type: Text Stats: 2204 properties of this type in this kind.
      import_transform: db.Text


#- kind: SurveyAssignment
#  connector: csv
#  connector_options:
#    encoding: utf-8
#    columns: from_header 
#  property_map:
#    - property: __key__
#      external_name: key
#      export_transform: transform.key_id_or_name_as_string
#      import_transform: transform.create_foreign_key('SurveyAssignment', key_is_id=True)
#
#    - property: createdDateTime
#      external_name: createdDateTime
#      # Type: Date/Time Stats: 30 properties of this type in this kind.
#      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
#      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')
#
#    - property: deviceIds
#      external_name: deviceIds
#      # Type: Integer Stats: 49 properties of this type in this kind.
#      import_transform: transform.none_if_empty(int)
#
#    - property: endDate
#      external_name: endDate
#      # Type: Date/Time Stats: 30 properties of this type in this kind.
#      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
#      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')
#
#    - property: language
#      external_name: language
#      # Type: String Stats: 13 properties of this type in this kind.
#
#    - property: lastUpdateDateTime
#      external_name: lastUpdateDateTime
#      # Type: Date/Time Stats: 30 properties of this type in this kind.
#      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
#      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')
#
#    - property: name
#      external_name: name
#      # Type: String Stats: 30 properties of this type in this kind.
#
#    - property: startDate
#      external_name: startDate
#      # Type: Date/Time Stats: 30 properties of this type in this kind.
#      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
#      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')
#
#    - property: surveyIds
#      external_name: surveyIds
#      # Type: Integer Stats: 51 properties of this type in this kind.
#      import_transform: transform.none_if_empty(int)


- kind: SurveyAttributeMapping
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyAttributeMapping', key_is_id=True)

    - property: apTypes
      external_name: apTypes
      # Type: String Stats: 32 properties of this type in this kind.

    - property: attributeName
      external_name: attributeName
      # Type: String Stats: 32 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 32 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 32 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: objectName
      external_name: objectName
      # Type: String Stats: 32 properties of this type in this kind.

    - property: questionGroupId
      external_name: questionGroupId
      # Type: Integer Stats: 32 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyId
      external_name: surveyId
      # Type: Integer Stats: 32 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyQuestionId
      external_name: surveyQuestionId
      # Type: String Stats: 32 properties of this type in this kind.


- kind: SurveyContainer
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyContainer', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 11 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 11 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: surveyDocument
      external_name: surveyDocument
      # Type: Text Stats: 11 properties of this type in this kind.
      import_transform: db.Text

    - property: surveyId
      external_name: surveyId
      # Type: Integer Stats: 11 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: SurveyGroup
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyGroup', key_is_id=True)

    - property: code
      external_name: code
      # Type: String Stats: 14 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 14 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: description
      external_name: description
      # Type: String Stats: 10 properties of this type in this kind.

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 14 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: SurveyInstanceSummary
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyInstanceSummary', key_is_id=True)

    - property: collectionDate
      external_name: collectionDate
      # Type: Date/Time Stats: 1810 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: communityCode
      external_name: communityCode
      # Type: String Stats: 95 properties of this type in this kind.

    - property: count
      external_name: count
      # Type: Integer Stats: 1810 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: countryCode
      external_name: countryCode
      # Type: String Stats: 1810 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 1810 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 1810 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: SurveyInstance
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyInstance', key_is_id=True)

    - property: approvedFlag
      external_name: approvedFlag
      # Type: String Stats: 76 properties of this type in this kind.

    - property: approximateLocationFlag
      external_name: approximateLocationFlag
      # Type: String Stats: 1 properties of this type in this kind.

    - property: collectionDate
      external_name: collectionDate
      # Type: Date/Time Stats: 1740 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: countryCode
      external_name: countryCode
      # Type: String Stats: 1682 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 1740 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: deviceFileId
      external_name: deviceFileId
      # Type: Integer Stats: 1739 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: deviceIdentifier
      external_name: deviceIdentifier
      # Type: String Stats: 1654 properties of this type in this kind.

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 1740 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: sublevel1
      external_name: sublevel1
      # Type: String Stats: 90 properties of this type in this kind.

    - property: sublevel2
      external_name: sublevel2
      # Type: String Stats: 90 properties of this type in this kind.

    - property: submitterName
      external_name: submitterName
      # Type: String Stats: 1654 properties of this type in this kind.

    - property: surveyId
      external_name: surveyId
      # Type: Integer Stats: 1740 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyedLocaleId
      external_name: surveyedLocaleId
      # Type: Integer Stats: 1684 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: userID
      external_name: userID
      # Type: Integer Stats: 1740 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: uuid
      external_name: uuid
      # Type: String Stats: 1715 properties of this type in this kind.


- kind: SurveyMetricMapping
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyMetricMapping', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 4 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 4 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: metricId
      external_name: metricId
      # Type: Integer Stats: 4 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: questionGroupId
      external_name: questionGroupId
      # Type: Integer Stats: 4 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyId
      external_name: surveyId
      # Type: Integer Stats: 4 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyQuestionId
      external_name: surveyQuestionId
      # Type: Integer Stats: 4 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: SurveyQuestionSummary
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyQuestionSummary', key_is_id=True)

    - property: count
      external_name: count
      # Type: Integer Stats: 17900 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 17900 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 17900 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: questionId
      external_name: questionId
      # Type: String Stats: 17900 properties of this type in this kind.

    - property: response
      external_name: response
      # Type: String Stats: 17900 properties of this type in this kind.


- kind: Survey
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Survey', key_is_id=True)

    - property: code
      external_name: code
      # Type: String Stats: 36 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 36 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: defaultLanguageCode
      external_name: defaultLanguageCode
      # Type: String Stats: 23 properties of this type in this kind.

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 36 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: String Stats: 36 properties of this type in this kind.

    - property: path
      external_name: path
      # Type: String Stats: 36 properties of this type in this kind.

    - property: pointType
      external_name: pointType
      # Type: String Stats: 22 properties of this type in this kind.

    - property: requireApproval
      external_name: requireApproval
      # Type: Boolean Stats: 24 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: status
      external_name: status
      # Type: String Stats: 32 properties of this type in this kind.

    - property: surveyGroupId
      external_name: surveyGroupId
      # Type: Integer Stats: 36 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: version
      external_name: version
      # Type: Float Stats: 35 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)


- kind: SurveyalValue
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyalValue', key_is_id=True)

    - property: collectionDate
      external_name: collectionDate
      # Type: Date/Time Stats: 64944 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: countryCode
      external_name: countryCode
      # Type: String Stats: 64850 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 64944 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: day
      external_name: day
      # Type: Integer Stats: 64944 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 64944 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: localeType
      external_name: localeType
      # Type: String Stats: 8391 properties of this type in this kind.

    - property: metricGroup
      external_name: metricGroup
      # Type: String Stats: 203 properties of this type in this kind.

    - property: metricId
      external_name: metricId
      # Type: Integer Stats: 203 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: metricName
      external_name: metricName
      # Type: String Stats: 203 properties of this type in this kind.

    - property: month
      external_name: month
      # Type: Integer Stats: 64944 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: numericValue
      external_name: numericValue
      # Type: Float Stats: 8851 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: organization
      external_name: organization
      # Type: String Stats: 8675 properties of this type in this kind.

    - property: questionText
      external_name: questionText
      # Type: String Stats: 64818 properties of this type in this kind.

    - property: questionType
      external_name: questionType
      # Type: String Stats: 64818 properties of this type in this kind.

    - property: stringValue
      external_name: stringValue
      # Type: String Stats: 64944 properties of this type in this kind.

    - property: sublevel1
      external_name: sublevel1
      # Type: String Stats: 302 properties of this type in this kind.

    - property: sublevel2
      external_name: sublevel2
      # Type: String Stats: 302 properties of this type in this kind.

    - property: surveyInstanceId
      external_name: surveyInstanceId
      # Type: Integer Stats: 64944 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyQuestionId
      external_name: surveyQuestionId
      # Type: Integer Stats: 64818 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: surveyedLocaleId
      external_name: surveyedLocaleId
      # Type: Integer Stats: 64944 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: valueType
      external_name: valueType
      # Type: String Stats: 64944 properties of this type in this kind.

    - property: year
      external_name: year
      # Type: Integer Stats: 64944 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: SurveyedLocale
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('SurveyedLocale', key_is_id=True)

    - property: ambiguous
      external_name: ambiguous
      # Type: Boolean Stats: 1177 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: countryCode
      external_name: countryCode
      # Type: String Stats: 1175 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 1177 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: currentStatus
      external_name: currentStatus
      # Type: String Stats: 1009 properties of this type in this kind.

    - property: identifier
      external_name: identifier
      # Type: String Stats: 1177 properties of this type in this kind.

    - property: lastSurveyalInstanceId
      external_name: lastSurveyalInstanceId
      # Type: Integer Stats: 1177 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: lastSurveyedDate
      external_name: lastSurveyedDate
      # Type: Date/Time Stats: 1177 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 1177 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: latitude
      external_name: latitude
      # Type: Float Stats: 1177 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: localeType
      external_name: localeType
      # Type: String Stats: 198 properties of this type in this kind.

    - property: longitude
      external_name: longitude
      # Type: Float Stats: 1177 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: organization
      external_name: organization
      # Type: String Stats: 237 properties of this type in this kind.

    - property: sublevel1
      external_name: sublevel1
      # Type: String Stats: 89 properties of this type in this kind.

    - property: sublevel2
      external_name: sublevel2
      # Type: String Stats: 89 properties of this type in this kind.


- kind: Translation
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('Translation', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 667 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: languageCode
      external_name: languageCode
      # Type: String Stats: 667 properties of this type in this kind.

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 667 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: parentId
      external_name: parentId
      # Type: Integer Stats: 667 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: parentType
      external_name: parentType
      # Type: String Stats: 667 properties of this type in this kind.

    - property: text
      external_name: text
      # Type: String Stats: 667 properties of this type in this kind.


- kind: UnitOfMeasure
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('UnitOfMeasure', key_is_id=True)

    - property: code
      external_name: code
      # Type: String Stats: 13 properties of this type in this kind.

    - property: system
      external_name: system
      # Type: String Stats: 13 properties of this type in this kind.


- kind: UserConfig
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('UserConfig', key_is_id=True)

    - property: config_INTEGER_IDX
      external_name: config_INTEGER_IDX
      # Type: Integer Stats: 68 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: group
      external_name: group
      # Type: String Stats: 68 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 68 properties of this type in this kind.

    - property: value
      external_name: value
      # Type: String Stats: 68 properties of this type in this kind.


- kind: User
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('User', key_is_id=True)

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 35 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: emailAddress
      external_name: emailAddress
      # Type: String Stats: 34 properties of this type in this kind.

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 35 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: permissionList
      external_name: permissionList
      # Type: String Stats: 30 properties of this type in this kind.

    - property: superAdmin
      external_name: superAdmin
      # Type: Boolean Stats: 2 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: userName
      external_name: userName
      # Type: String Stats: 34 properties of this type in this kind.


- kind: WebActivityAuthorization
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('WebActivityAuthorization', key_is_id=True)

    - property: authType
      external_name: authType
      # Type: String Stats: 3 properties of this type in this kind.

    - property: createdDateTime
      external_name: createdDateTime
      # Type: Date/Time Stats: 3 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: expirationDate
      external_name: expirationDate
      # Type: Date/Time Stats: 3 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdateDateTime
      external_name: lastUpdateDateTime
      # Type: Date/Time Stats: 3 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: name
      external_name: name
      # Type: String Stats: 3 properties of this type in this kind.

    - property: payload
      external_name: payload
      # Type: String Stats: 3 properties of this type in this kind.

    - property: token
      external_name: token
      # Type: String Stats: 3 properties of this type in this kind.

    - property: usageCount
      external_name: usageCount
      # Type: Integer Stats: 3 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: userId
      external_name: userId
      # Type: Integer Stats: 3 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: userName
      external_name: userName
      # Type: String Stats: 2 properties of this type in this kind.

    - property: webActivityName
      external_name: webActivityName
      # Type: String Stats: 3 properties of this type in this kind.


#- kind: _ah_SESSION
#  connector: csv
#  connector_options:
#    encoding: utf-8
#    columns: from_header 
#  property_map:
#    - property: __key__
#      external_name: key
#      export_transform: transform.key_id_or_name_as_string
#      import_transform: transform.create_foreign_key('_ah_SESSION', key_is_id=True)
#
#    - property: _expires
#      external_name: _expires
#      # Type: Integer Stats: 20 properties of this type in this kind.
#      import_transform: transform.none_if_empty(int)
#
#    - property: _values
#      external_name: _values
#      # Type: Blob Stats: 20 properties of this type in this kind.
#      import_transform: transform.blobproperty_from_base64
#      export_transform: base64.b64encode
#
